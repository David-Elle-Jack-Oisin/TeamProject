#include <iostream>
#include <stdio.h>
#include "raylib.h"

class Bullet {
    private:
    Vector2 position;
    Vector2 speed;
    float radius;
    float rotation;
    int lifeSpan;
    int shotDamage = 1;
    bool active;

    public:

        if (IsKeyPressed(KEY_SPACE)){
            for (int i = 0; i < 10; i++)
                {
                    if (!shoot[i].active)
                    {
                        shoot[i].position = (Vector2) player->position.x , player->position.y
                        shoot[i].active = true;
                        shoot[i].speed.x = 
                        shoot[i].speed.y = 
                        break;
                    }
                }
            }
};